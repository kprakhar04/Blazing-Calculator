const styles = theme => ({
    root: {
        flexGrow: 1,
    },
    container: {
        margin: "auto",
        width: "90%",
        cursor: "pointer"
    },
    heading: {
        marginTop: "10px",
        marginLeft: "80px",
        color: "#2a3d66"
    }
});
export default styles